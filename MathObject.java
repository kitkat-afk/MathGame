public class MathObject {
    private int one, two, sum;
    private String op;

    public MathObject(digitOne, operand, digitTwo, result)
    {
        one = digitOne;
        op = operand;
        two = digitTwo;
        sum = result;
    }
}